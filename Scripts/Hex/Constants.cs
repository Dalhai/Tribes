using Godot;

namespace TribesOfDust.Hex
{
    public static class HexConstants 
    {
        // Note: DefaultSize, DefaultWidth, DefaultHeight, and DefaultRatio have been removed.
        // Use TileSet extension methods for size-based calculations instead.
    }
}